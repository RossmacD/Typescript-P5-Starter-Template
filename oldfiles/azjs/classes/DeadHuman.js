'use strict';
import Human from './abstract/Human';

export default class DeadHuman extends Human {
    // Constructor takes in a Human object to replace
    constructor(_human) {
        super();
    }
}